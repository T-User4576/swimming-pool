# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

**See [`AGENTS.md`](./AGENTS.md).** It is the single source of truth for this
repository's context — binding architecture decisions, per-folder conventions,
pinned versions, deployment commands, and anti-patterns. Read it before any task.

Note on language:
- **German** for prose documentation (`README.md`, design/architecture `*.md`).
- **English** for comments in code and deployment artefacts (`*.py`, `*.sh`,
  `*.sql`, `Dockerfile`, `values*.yaml`, Helm templates, K8s manifests,
  ConfigMaps, Secrets, Argo workflow YAML, pipeline specs).
